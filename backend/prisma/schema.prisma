generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// No models required for connectivity health check; add models here as needed.
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
