generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

  emembedding JSONB  embedding Json
  embedding JSONB
embedding JSONBJson
  createdAt DateTime @default(now())
